package codehere.examples;

// created a functional interface(FuctIterface) to use for multiply
@FunctionalInterface
public interface FuctIterface {
	
	// add abstract method
	public int multiply(int a, int b);

}
